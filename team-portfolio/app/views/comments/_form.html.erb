<%= form_for(Comment.new, url: create_comment_path) do |f| %>
    <%= f.hidden_field :page, value: page_name %>
    <p>
      <%= f.label :content %><br>
      <%= f.text_area :content %>
    </p>
    <p>
      <%= f.submit %>
    </p>
  <% end %>
  